{"title":"Args parser: review checklist","gitnotes":1,"type":"text","body":"Review summary for argv handling commit.\n\nWhat changed\n- src/args.ts parses --config, --json, --help, and positional subcommands.\n- Defaults applied before flag overrides (see parseArgs return).\n\nWhere to look first\n1. parseArgs() — main entry, ~line 12\n2. printHelp() — usage string matches README\n3. normalizePath() — tilde expansion stub (intentionally naive)\n\nRisk\n- Unknown flags silently ignored today; commit 30 adds warnings.\n- No test coverage yet (lands in commit 29).\n\nVerdict: safe scaffolding."}
