{"title":"CLI wiring: pairing context on #42","gitnotes":1,"type":"text","body":"Co-author note — written while pairing on issue #42.\n\nConstraint: keep cli.ts under 60 lines so the Hub diff pane fits one screen without scrolling in screenshots.\n\nWhat changed: cli.ts orchestrates parseArgs → loadConfig → runCommand → formatOutput.\n\nAlex handled the error exit codes; I wired the import graph. We split on whether --json should bypass formatOutput entirely (decided yes, implemented in commit 28).\n\nLook at main() first, then the runCommand switch — only \"status\" subcommand is implemented; others stub to exit 2."}
