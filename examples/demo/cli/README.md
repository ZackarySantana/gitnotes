# GitNotes Demo CLI

Small fictional TypeScript CLI demonstrating args parsing, config loading,
and formatted output. Part of the `examples/demo/cli` slice on the demo branch.

## Status

Scaffold only — implementation lands in subsequent commits.
